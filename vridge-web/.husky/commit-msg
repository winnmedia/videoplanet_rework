#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Validate commit message format using commitlint - DISABLED FOR DEPLOYMENT
echo "📝 Validating commit message format... (SKIPPED)"
# pnpm dlx commitlint --edit "$1"